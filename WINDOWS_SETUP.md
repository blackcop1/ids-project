# WINDOWS SETUP GUIDE - Complete Instructions\n\n## ❌ Problem: PowerShell Execution Policy\n\nYou're seeing this error:\n```\nrunning scripts is disabled on this system\n```\n\nThis is a Windows security feature. Easy fix!\n\n---\n\n## ✅ SOLUTION 1: Use Command Prompt (EASIEST)\n\n**Close PowerShell and open Command Prompt (cmd.exe) instead:**\n\n```cmd\n# 1. Navigate to project\ncd D:\\MY SUMMER PROJECTS\\ids-project\n\n# 2. Create virtual environment\npython -m venv venv\n\n# 3. ACTIVATE - Command Prompt way\nvenv\\Scripts\\activate.bat\n\n# You should see (venv) at the start of your command line\n\n# 4. Upgrade pip, setuptools, wheel\npython -m pip install --upgrade pip setuptools wheel\n\n# 5. Install requirements\npip install -r requirements.txt\n```\n\n**This is the fastest solution!** ✅\n\n---\n\n## ✅ SOLUTION 2: Allow PowerShell Scripts (If you prefer PowerShell)\n\nOpen **PowerShell as Administrator** and run:\n\n```powershell\n# Allow scripts for current user only\nSet-ExecutionPolicy -ExecutionPolicy RemoteSignedCurrentUser -Scope CurrentUser -Force\n\n# Then you can use:\nvenv\\Scripts\\activate\n```\n\n**Then run the normal setup:**\n\n```powershell\ncd \"D:\\MY SUMMER PROJECTS\\ids-project\"\npython -m venv venv\nvenv\\Scripts\\activate\npython -m pip install --upgrade pip setuptools wheel\npip install -r requirements.txt\n```\n\n---\n\n## ✅ SOLUTION 3: Bypass for This Session Only (Temporary)\n\nIf you just want to activate the virtual environment once:\n\n```powershell\n# Bypass execution policy for this session only\npowershell -ExecutionPolicy Bypass -NoExit -Command \"& {cd 'D:\\MY SUMMER PROJECTS\\ids-project'; .\\venv\\Scripts\\activate}\"\n```\n\n---\n\n## 🎯 RECOMMENDED: Use Command Prompt (cmd.exe)\n\n**Why?**\n- ✅ No PowerShell issues\n- ✅ Works on all Windows versions\n- ✅ No administrator needed\n- ✅ Fewer compatibility issues\n\n### Step-by-Step:\n\n1. **Close PowerShell**\n\n2. **Open Command Prompt (cmd.exe)**\n   - Press `Win + R`\n   - Type `cmd`\n   - Press Enter\n\n3. **Copy & Paste These Commands:**\n\n```cmd\ncd /d D:\\MY SUMMER PROJECTS\\ids-project\npython -m venv venv\nvenv\\Scripts\\activate.bat\npython -m pip install --upgrade pip setuptools wheel\npip install -r requirements.txt\n```\n\n4. **Wait for installation to complete**\n\n5. **Test it:**\n\n```cmd\npython -c \"import numpy; import pandas; print('✅ SUCCESS')\"\n```\n\n---\n\n## 📋 What Each Command Does\n\n| Command | What It Does |\n|---------|-------------|\n| `cd /d D:\\...` | Navigate to your project folder |\n| `python -m venv venv` | Create isolated Python environment |\n| `venv\\Scripts\\activate.bat` | Activate the environment (Command Prompt) |\n| `python -m pip install --upgrade pip setuptools wheel` | Install build tools for Python 3.13 |\n| `pip install -r requirements.txt` | Install all project dependencies |\n\n---\n\n## 🚀 After Setup is Complete\n\nOnce you see `(venv)` in your command prompt:\n\n```cmd\n# Download dataset\npython data/download_dataset.py\n\n# Train models\npython main.py\n\n# Open Jupyter\njupyter notebook notebooks/EDA_and_Model_Comparison.ipynb\n```\n\n---\n\n## ⚡ Command Prompt Quick Reference\n\n```cmd\n# See current folder contents\ndir\n\n# Go to a folder\ncd folder_name\n\n# Go back one folder\ncd ..\n\n# Go to specific drive\nD:\n\n# Go to specific path\ncd /d D:\\path\\to\\folder\n\n# List Python packages\npip list\n\n# Deactivate virtual environment\ndeactivate\n\n# Clear screen\ncls\n```\n\n---\n\n## 🆘 Still Having Issues?\n\n### Issue: \"python command not found\"\n**Fix:** Make sure Python is in your PATH\n```cmd\npython --version\n```\nIf this doesn't work, reinstall Python and check \"Add to PATH\"\n\n### Issue: \"pip not found\"\n**Fix:** Use `python -m pip` instead\n```cmd\npython -m pip install -r requirements.txt\n```\n\n### Issue: Permission denied\n**Fix:** Run Command Prompt as Administrator\n- Press `Win + R`\n- Type `cmd`\n- Press `Ctrl + Shift + Enter` (runs as admin)\n\n### Issue: Virtual environment not activating\n**Fix:** Make sure you're using the correct command:\n```cmd\n# CORRECT for Command Prompt:\nvenv\\Scripts\\activate.bat\n\n# WRONG (this is for Linux/Mac):\nvenv/bin/activate\n```\n\n---\n\n## 📝 Complete Setup Script (Copy All at Once)\n\nCreate a file named `setup.bat` in your project folder with this content:\n\n```batch\n@echo off\necho Installing IDS Project...\n\necho Step 1: Creating virtual environment...\npython -m venv venv\n\necho Step 2: Activating virtual environment...\ncall venv\\Scripts\\activate.bat\n\necho Step 3: Upgrading pip and build tools...\npython -m pip install --upgrade pip setuptools wheel\n\necho Step 4: Installing requirements...\npip install -r requirements.txt\n\necho.\necho ✅ Setup Complete!\necho.\necho To activate the environment in future:\necho   venv\\Scripts\\activate.bat\necho.\npause\n```\n\nThen double-click `setup.bat` to run everything automatically!\n\n---\n\n## ✅ Final Verification\n\nAfter installation, test everything:\n\n```cmd\n# Verify Python\npython --version\n\n# Verify pip\npip --version\n\n# Verify key packages\npython -c \"import numpy, pandas, sklearn, xgboost; print('✅ All packages working!')\"\n\n# List all installed packages\npip list\n```\n\n---\n\n## 🎯 TL;DR (Too Long; Didn't Read)\n\n**Just do this:**\n\n1. Open **Command Prompt** (not PowerShell)\n2. Run:\n   ```cmd\n   cd /d \"D:\\MY SUMMER PROJECTS\\ids-project\"\n   python -m venv venv\n   venv\\Scripts\\activate.bat\n   python -m pip install --upgrade pip setuptools wheel\n   pip install -r requirements.txt\n   ```\n3. Done! ✅\n\n---\n\n**Need help?** Let me know and I can provide additional support! 🚀\n"